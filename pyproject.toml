[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "semantic-segmentation-benchmark"
version = "0.1.0"

[tool.setuptools]
packages = ["semantic_segmentation_benchmark"]
